package ua.notebook_shop.model;

import javax.persistence.MappedSuperclass;

@MappedSuperclass
public abstract class Element extends IdGenerate{
    /*This interface was created to combine
    all the elements in one group
     */
}
